<view class="flex" bind:tap="gotoArtist">
    <view>
        <image class="artist-pic" src="{{item.picUrl}}" />
    </view>
    <view class="artist-name">{{item.name}}</view>
    <view class="artist-other" wx:for="{{item.alia}}" wx:for-item="i" wx:key="index">({{i}})</view>
</view>